#include <ioManager.h>

/* uint32_t readHandler(int fd){
    if(fd == STDIN)
        
} */

/* uint32_t writeHandler(int fd, char * str){
    if(fd == STDOUT)
        ncPrint(str);
    else
        writePipe()
} */